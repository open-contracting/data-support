{
  "name": "ids-drr-branding",
  "version": "0.1.0",
  "private": true,
  "description": "Idris (Paraguay) branding package — ships as the same module name as the stub so the frontend can swap implementations without code changes.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": [
    "**/*.css",
    "**/*.scss"
  ],
  "packageManager": "pnpm@11.9.0",
  "dependencies": {
    "ids-drr-branding-types": "github:open-contracting/ids-drr-branding-types"
  },
  "devDependencies": {
    "knip": "^6.23.0"
  },
  "peerDependencies": {
    "next": "^15",
    "next-intl": "^3.4",
    "opub-ui": "^0.5",
    "react": "^19",
    "react-dom": "^19"
  }
}
